name: Meu Primeiro Workflow no Actions

on:
  # definir triggers
  push: 
    branches: ["main"]
  pull_request:
    branches: ["main"]

  #habilitar para disparo manual
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: run a one-line scsript
        run: echo ola, turma da disciplina git actions.

      
      

  
